# Open Problems

Each subdirectory is an active problem with Lean source and working notes.

## Adding a Problem

1. Create a directory: `problems/<name>/`
2. Add `PROBLEM.md` with the problem statement
3. Add `Lean/` directory with a Lean skeleton (sorrys for open obligations)
4. Add `notes/` directory for NL exploration
5. Open issues for each sorry
